<%= form_for(@newSavedMeal) do |f| %>

  <center>
  <div class="field">
    <%= f.text_field :content, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit "Save Meal" %>
  </div>
</center>
<% end %>
